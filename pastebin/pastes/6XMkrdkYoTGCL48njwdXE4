docker
How to push image from local to docker hub:

docker  tag <source-image>:<tag>  <dockerhub-username/repository-name>:tagname
docker tag personal-portfolio:v1 docker7028/dockerepo-list:personal-portfolio

 docker tag cal-application:latest docker7028/docker-hub-repo:cal-application


docker push docker7028/dockerepo-list:tagname 
docker push docker7028/dockerepo-list:personal-portfolio

 docker push docker7028/docker-hub-repo:cal-application  

docker tag local-image:tagname new-repo:tagname
docker push new-repo:tagname